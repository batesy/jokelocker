<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "JokeLocker" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar small" data-topbar >
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to "JokeLocker", root_path %></h1>
        </li>
      </ul>

      <section class="top-bar-section">
        <% if current_user %>
        <ul class="right">
          <li class="has-dropdown">
            <a href="/users/<%= current_user.id %>"><%= current_user.username %></a>
            <ul class="dropdown">
              <li>
                <%= link_to "Logout", :logout, method: :post %>
              </li>
            </ul>
          </li>
        </ul>
        <% else %>
        <ul class="right">
          <li class="active">
            <%= link_to "Sign Up", new_user_path %>
          </li>
          <li>
            <%= link_to "Login", :login %>
          </li>
        </ul>
        <% end %>
      </section>
    </nav>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
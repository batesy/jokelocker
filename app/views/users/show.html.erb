<div class="row">
  <div class="small-12 column">
    <h1><%= current_user.username %>'s Jokes</h1>
  </div>
</div>
  <% @jokes.each do |joke| %>
<div class="row">
  <div class="small-12 columns">
    <div class="panel"><%= link_to Joke.find(joke.joke_id).title, "/jokes/#{joke.joke_id}" %></div>
  </div>
</div>
<% end %>

